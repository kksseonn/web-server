# Web Server

Приложение запускается через Docker Compose: веб-приложение Flask и PostgreSQL поднимаются вместе.

## Что нужно

- Docker
- Docker Compose

## Быстрый старт

Сборка и запуск:

```bash
docker compose up --build -d
```

Проверить контейнеры:

```bash
docker compose ps
```

## Тестовые пользователи

- `admin / admin123` — администратор, может добавлять, редактировать и удалять записи сотрудников.
- `user / user123` — обычный пользователь, имеет доступ только на чтение таблицы.

## Полезные команды

Посмотреть логи:

```bash
docker compose logs -f
```

Посмотреть логи только приложения:

```bash
docker compose logs -f web
```

Остановить контейнеры:

```bash
docker compose down
```

Остановить контейнеры и удалить volume базы данных:

```bash
docker compose down -v
```

Повторно пересобрать и запустить после изменений:

```bash
docker compose up --build -d
```

## Переменные окружения

Сейчас они заданы прямо в `docker-compose.yml`:

- `DATABASE_URL=postgresql://app_user:app_password@db:5432/app_db`
- `SECRET_KEY=replace-this-secret`
